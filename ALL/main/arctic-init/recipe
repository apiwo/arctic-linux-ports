# arctic-init - The Arctic init scripts and service manager
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=arctic-init
version=1.0.0
release=1
desc="The Arctic init scripts and service manager"
url="https://github.com/apiwo/arctic-linux"
license="BSD-2-Clause"
depend="busybox arctic-base"
makedepend=""
source=""
sha256=""

# A meta package: the dependency list is the whole payload. Installing it pulls
# in a working setup and nothing else.
package() {
	install -d "$pkgdir/usr/share/arctic/meta"
	printf '%s\n' "busybox arctic-base" >"$pkgdir/usr/share/arctic/meta/arctic-init"
}
