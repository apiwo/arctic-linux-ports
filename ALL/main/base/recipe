# base - A minimal but complete Arctic system
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=base
version=1.0.0
release=1
desc="A minimal but complete Arctic system"
url="https://github.com/apiwo/arctic-linux"
license="BSD-2-Clause"
depend="glibc busybox toybox zsh doas alpm arctic-base arctic-init libarchive mandoc onetrueawk libxcrypt iana-etc tzdata ca-certificates"
makedepend=""
source=""
sha256=""

# A meta package: the dependency list is the whole payload. Installing it pulls
# in a working setup and nothing else.
package() {
	install -d "$pkgdir/usr/share/arctic/meta"
	printf '%s\n' "glibc busybox toybox zsh doas alpm arctic-base arctic-init libarchive mandoc onetrueawk libxcrypt iana-etc tzdata ca-certificates" >"$pkgdir/usr/share/arctic/meta/base"
}
