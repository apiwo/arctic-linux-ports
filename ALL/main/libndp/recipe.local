Marker only - see CONTRIBUTING.md. build() runs autoreconf first (GitHub's
tag archive has no generated ./configure) and gen-ports.py must never
regenerate over that from the manifest's generic autotools template.
