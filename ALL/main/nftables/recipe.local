# --with-cli=no is passed in build(): configure probes for "readline" inside
# -ledit, which Arctic's libedit does not provide, and without it nftables
# cannot be packaged at all. The template has no field for extra options.
