# difftastic - A diff that understands syntax
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=difftastic
version=0.65.0
release=1
desc="A diff that understands syntax"
url="https://difftastic.wilfred.me.uk/"
license="MIT"
depend="glibc"
makedepend="rust cargo"
source="https://github.com/Wilfred/difftastic/archive/refs/tags/0.65.0.tar.gz"
sha256="SKIP"

build() {
	cd "0.65.0"
	# Arctic builds Rust offline against a vendored tree so packages are
	# reproducible and the build host needs no network.
	export RUSTFLAGS="-C linker=clang -C link-arg=-fuse-ld=lld"
	cargo build --release --locked --offline
}

package() {
	cd "0.65.0"
	install -Dm755 "target/release/difftastic" "$pkgdir/usr/bin/difftastic"
	for d in README.md README LICENSE; do
		[ -f "$d" ] && install -Dm644 "$d" "$pkgdir/usr/share/doc/difftastic/$d"
	done
	true
}
